{
  "name": "nbio",
  "version": "1.0.0",
  "private": true,
  "description": "NBIO Tracker — JS test harness (production JS is served straight from app/nbio/static/, no build step).",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.0",
    "jsdom": "^25.0.0",
    "fake-indexeddb": "^6.0.0"
  }
}
